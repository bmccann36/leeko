{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "lerna exec -- npm i",
    "bootstrap": "lerna bootstrap",
    "runGql": "lerna run --scope gqlserver start --stream"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-mocha": "^6.3.0",
    "mocha": "^7.1.1"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "aws-sdk": "^2.652.0",
    "lerna": "^3.20.2",
    "uuidv4": "^6.0.7"
  }
}
